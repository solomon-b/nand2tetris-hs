-- |
module Interpreter
  (
  ) where

--------------------------------------------------------------------------------

import Parser.Syntax qualified as Syntax

eval :: Syntax.Chip -> Bool
eval = undefined
